function btn_clicked(i) {
    const buttonId = i;
    console.log('Button clicked with ID:', buttonId);
    window.location.href = "/predict2/" + buttonId;
  }

  function btn_clicked1(i) {
    const buttonId = i;
    console.log('Button clicked with ID:', buttonId);
    window.location.href = "/predict3/" + buttonId;
  }

  function btn_clicked2(i) {
    const buttonId = i;
    console.log('Button clicked with ID:', buttonId);
    window.location.href = "/predict4/" + buttonId;
  }
   
  function btn_clicked3(i) {
    const buttonId = i;
    console.log('Button clicked with ID:', buttonId);
    window.location.href = "/predict5/" + buttonId;
  }